# Python 异步编程 `Python 3.7+`

探索 Python 的异步编程特性和模式。

> **语言版本**: Python 3.7 或更高（需要 `asyncio.run()` 支持）

## 包含内容

1. 基础协程
2. 并发执行 (gather, create_task)
3. 异步上下文管理器
4. 异步生成器
5. 异步迭代器
6. 超时和取消

## 运行

```bash
python languages/python/async/main.py
```
